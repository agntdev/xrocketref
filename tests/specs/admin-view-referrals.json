[
  {
    "name": "View Recent Referrals shows empty state",
    "steps": [
      {
        "send": { "callback": "admin:view_referrals" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No referrals yet. Generate one to start tracking."
            }
          }
        ]
      }
    ]
  }
]
