[
  {
    "name": "Retry Failed Payout shows empty state",
    "steps": [
      {
        "send": { "callback": "payout:retry" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No failed payouts to retry."
            }
          }
        ]
      }
    ]
  }
]
