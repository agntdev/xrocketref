[
  {
    "name": "/generate_referral creates a referral code",
    "steps": [
      {
        "send": { "text": "/generate_referral" },
        "expect": [
          {
            "method": "sendMessage"
          }
        ]
      }
    ]
  },
  {
    "name": "Generate referral button works",
    "steps": [
      {
        "send": { "callback": "referral:generate" },
        "expect": [
          {
            "method": "editMessageText"
          }
        ]
      }
    ]
  }
]
